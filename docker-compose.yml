services:
  invitation-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/voice-chat-team/voice-chat-notification-service:latest
    networks:
      - voice-chat-network
    restart: unless-stopped

networks:
  voice-chat-network:
    external: true
